# Tuples :- A built-in data type that lets us create immutable sequences of values.
tup = (2, 1, 3)
print(tup)
print(type(tup))
print(tup[0])

tup1 = ()  # Empty Tuple
tup2 = (1,)  # Single Tuple

# Tuple Slicing
print(tup[1:3])
